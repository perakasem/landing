# Supabase Configuration
PUBLIC_SUPABASE_URL=your-project-url.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role key - ONLY for migrations and server-side admin operations
# Has full database access, bypasses RLS - NEVER expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Directus Configuration (optional - if self-hosting)
DIRECTUS_URL=http://localhost:8055
DIRECTUS_ADMIN_EMAIL=admin@example.com
DIRECTUS_ADMIN_PASSWORD=your-secure-password

# Environment
NODE_ENV=development
